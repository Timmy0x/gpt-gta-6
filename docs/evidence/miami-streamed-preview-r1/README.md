This checkpoint adds a local Google Map Tiles / Cesium ion source viewer and a separate coordinate regression fixture. It does not load proprietary Miami data, replace the game map or implement gameplay collision.

The exact proposed committed tree was reconstructed from `3e14e075222549de4826cfa71b5f1ff7a5fcd0e8` plus explicitly selected files in `.local-builds/miami-streamed-tools-r1-clean`. Fresh locked installations passed the root production build, the preview's 13 tests and production build, and the coordinate fixture's generation, 5 tests and typecheck. The existing large-Babylon-chunk build advisory remains.

`clean-build/result.json` records 15 normal browser checks on that clean preview, with the production entry and SHA256. They cover disconnected startup, empty credentials, fixture loading, both tile credits, camera presets and mouse orbit, disconnect, missing root/child and retries, recovery, locally intercepted Google/ion403 responses, and an intercepted ion-to-Google401→refresh→200 sequence. That recovered path preserves both tile credits, the endpoint credit and Google Maps branding. All provider responses in this automated audit were synthetic and intercepted; no provider credentials or live Miami API access were used. There are zero uncaught page errors. Expected fault-case HTTP errors and the exact upstream 3D Tiles1.1 advisory are retained in the log rather than hidden.

The parent visually inspected `clean-build/synthetic-fixture.png` and `synthetic-overview.png` after shader warm-up. Both original test boxes are visible at420m and1500m camera radii. Initial metadata-only checks produced an early blank screenshot even though both tiles were loaded; the preserved `initial-attempt/result.json` also records the now-fixed favicon404. Tile coverage and shader-ready rasterization are therefore explicitly separate. The UI reports coverage; it does not promise immediate visible pixels from a load event.

The actual Google Brickell coverage was separately inspected in Babylon's official public demo using its coordinate controls, as recorded in `docs/continuation.md`. That source demonstration is not this local viewer or the playable game. Local live Miami rendering still requires the user's own provider access.

Run the preview using `tools/miami-source-preview/README.md`. From the repository root after installing its development dependencies, rerun the browser audit with `AUDIT_URL=http://127.0.0.1:4393 AUDIT_OUTPUT=/tmp/miami-source-audit node tests/miami-source-controls-audit.mjs`. The audit uses Chrome and only synthetic provider responses. It must not be pointed at a session containing real credentials.
