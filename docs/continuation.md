# Current checkpoint — geographic source access, 2026-09-12

User selected Aerometrex for personal use, has no delivery/download link, then asked why Google Earth or Apple Maps cannot be used. Provider contact is NOT authorized: user questioned that route instead of approving it. Root answered Google Photorealistic 3D Tiles is a viable streamed visual source and is preparing a concrete isolated preview; Apple MapKit has no documented mesh feed. No source model has been acquired, no order placed, and no provider message sent. The original US store URL redirects to the current quote catalogue; the offered free sample is Denver. Root and agents are checking partner routes and preparing an unsent Miami/Brickell request. Keep licensed deliveries and derived files local in ignored `.private-data/`, outside the public repository and Vercel. `docs/miami/aerometrex-access.md` records the access route and AOI. Delivery inspection and supplied-coordinate transforms are preparatory, not a completed importer or actual source integration.

**505915a** is pushed and Vercel **dpl_AmqubaEpXTjERpMYnAKWQ8UvmkDy** is **READY**. It fixes HTTP-decoded `.gz` weapon assets: Vite preview's Content-Encoding already decodes Fetch responses, so only gzip-magic archives need DecompressionStream. Three focused tests and an exact committed-tree fresh npm ci/build passed. `.local-builds/weapon-http-r1-clean` is the clean source, with manifest beside it. Production still uses the accepted R6 gameplay world.

Miami prototype R4 `.local-builds/miami-game-r4-source`, served on4298, passed **16 normal WebGPU controls** with zero errors/warnings: startup, walking, map, six named teleports, vehicle entry/driving/exit and time changes. Evidence `docs/evidence/miami-game-r4-webgpu/result.json`. It is a public-survey foundation, visibly incomplete and not photorealistically accepted. R1–R3 failed startup cohorts remain preserved. R5 is being prepared from R4 plus the strict shoreline fix; it clips inferred road buffers to dry source coverage and removes one wet lane endpoint rather than relaxing the water gate. Live export:36chunks395meshes2,559,292triangles84,838,544bytes; datasetSHA f193d78ab76ca2d6f10f847ee1025c09a4bdcbb1dc3b5b8ed19895537dc1fbc0. R5 passed all46 native travel cases,8,484 traffic samples with zero water,26 frozen focused tests and44 broader live Miami tests. Root normal browser audits passed44checkpoints each on WebGL and WebGPU, includingall34 named destinations, zeroerrors/warnings. Evidence under docs/evidence/miami-game-r5-{webgl,webgpu}. Visual acceptance remains open (plainCountyvolumes/genericland); do notpromote asfinishedphotorealism. Injury_ragdoll derived69-file future runtimecommit manifest/dependencyclosure in docs/evidence/miami-dry-coverage-r1; excludesoldMinihelperrollbacks andpreservesHEAD-only sources.

Aerometrex preparation: delivery-inspector R2 frozen11tests and transform R1 frozen6tests pass. No actualproviderfile. Road agent now owns isolated streamedpreview; injury_ragdoll audits renderer/game handedness and collisionintegration; injury_poses researchedcurrentGoogle/Cesium/Apple routes. See docs/miami/streamed-photogrammetry.md. Noaccountsignup,billing,credentialsorpurchase occurred.

Use exact accepted source cohorts for commits; current working tree includes unrelated unaccepted aircraft, responders and injury-animation edits. Preserve all of them. Push immediately after each tested commit. The full city/game goal is still active; no finished buildings or blocks are claimed.

# Continuation checkpoint

Full goal remains active. This is an early central-neighborhood build, not full scope completion. The user authorized frequent commits and pushes on 2026-09-09. Push every tested checkpoint to origin/main. The existing Vercel Git integration automatically deploys pushes to main to production; preserve that configured workflow.

## 2026-09-12 Vercel recovery

Latest user reported the most recent deployment failed. Root inspected Vercel build logs: committed injury tests imported `scripts/characters/interaction/frame-metrics.ts`, which had been omitted from a3b126a despite being present in its frozen test tree. **a47e283** commits that exact accepted helper and is pushed. Vercel deployment **dpl_J8y7yZyDwEKJ7DsvixP3jid2FgwM** reached **READY**, with production alias https://gpt-gta-6.vercel.app responding successfully. A fresh `git archive` source tree plus the helper passed actual `npm ci`, production build and all10 focused injury/anatomy tests. Independent closure audit covered235 tracked sources,17glTF assets/20 external refs,162 manifests/catalog refs and141 compressed assets; no additional runtime/build omissions. Runtime entry remains `index-BJRmjpwj.js`.

**Before every further commit, reconstruct the exact proposed committed tree and validate there.** A dirty source or frozen runtime comparison can hide omitted test helpers. Include all source, tests, scripts and asset dependency closures. Build evidence: `/tmp/vercel-fix-f58-{install,build,injury-tests}.log`; source `.local-builds/vercel-fix-f58-source`. No separate deploy command was needed.

## 2026-09-12 Miami rebuild — active priority

Latest user reiterated exact parity for every building/street/terrain/elevation, asked for a source to copy from, visible outputs and continued delegation. Current County2015 geometry is insufficient for architectural parity. Agent road_cars is researching full-city detailed downloadable survey/meshes; root researches Google/Cesium streaming separately in `docs/miami/streamed-photogrammetry.md`. Do not mistake a streamed visualization license for permission to extract permanent game assets. Show actual candidate renders and label unresolved inferred geometry.

**bfa8ab3** is now pushed and Vercel deployment **dpl_8f8TAqv2SzhrezDcSfQ5fVVEaBvr** is **READY**. This86-file checkpoint adds48 sourced PBR maps at1K/2K, material library, reference atlas, exact geometric footprint reconciliation, dependencies and Vercel recovery documentation. Exact proposed tree `.local-builds/miami-source-r3-clean` came from git archivea47e283 plus86 manifest files, ran freshnpmci/build,7focusednative tests and offline reconciliation successfully. Manifest `.local-builds/miami-source-r3-manifest.json`. Runtime stillR6oldmap.

Miami R2 browser startup also failed, now with `TypeError: Failed to fetch`,0checks; screenshot/result under`docs/evidence/miami-game-r2-webgpu`. Root added requestfailed/HTTP diagnostics and early startup failure detection for the next check. GPU temporarily belongs to injury_poses for frozen701R2 comparative render at4295. New701R2CPUgeometry6tests passes, separatecrown/roundedbody/podiumclosure,8independentwatertight/side/roof/corner checks. Inferredcorner/panepitchremain explicit. Residency correctness tests/fixes underway. Keep R2 runtime as a failed frozen diagnostic, not accepted state.

The latest user explicitly retired the fictional map and requested the City of Miami at1:1 scale, beginning with Brickell. See `docs/miami/rebuild-plan.md`. Full goal remains active; initial GIS geometry is not a completed photorealistic map. Preserve all character/vehicle/combat work and commit/push frequent tested checkpoints.

Newest pushed source checkpoint **f58aace** adds the67-file Miami source foundation (37.89MB) and rebuild plan. Offline byte/geometry/datum/identity checks and4 projection/normalization tests pass. Frozen source `.local-builds/miami-foundation-r1-source`; exact manifest `.local-builds/miami-foundation-r1-manifest.json`. Runtime replacement is still uncommitted/under validation; preview5174 remainsa3b126a.

Newest pushed gameplay checkpoint **a3b126a** integrates regional injuries, physical recovery and weapon handling. Exact source: `.local-builds/injury-game-r6-source`; commit manifest `.local-builds/injury-game-r6-commit-inputs.json`.230 native tests/type/build pass. Normal injury controls: WebGPU22; WebGL NPC16+player7 clean. On-foot weapon controls22 each: WebGPU clean, WebGL12 intermittent program warnings remain open; drive-by browser acceptance remains open. Accepted production entry `index-BJRmjpwj.js`, SHA256 `201705ae30ee44b6c7bbbd1d86b0cced48901d1e0d9425f8b41a0741dd1355e1`. Preview5174 now serves this checkpoint; Miami has not been promoted.

Miami team: road_cars acquires City/County/OSM source data and I3S building geometry; injury_ragdoll builds source roads/terrain/collision and width matcher; injury_poses owns surface materials, first-block references and authored exteriors. Root owns dataset/projection, runtime integration, saves/map/UI and integrated checks. Root currently owns the GPU browser for normal Miami startup/walking/map/travel/driving validation. Prototype R1 failed at startup because a stored native fetch function had the wrong receiver. R2 freezes the receiver correction in `.local-builds/miami-game-r2-source`, served on4291; its browser audit is in progress. Do not promote before acceptance. Agent injury_ragdoll independently audits residency lifecycle/collision readiness; geometry R3 has27passing native tests. Materials+atlas R1 and geodata reconciliation R2 are frozen, ready for source-only checkpoints; the first701 authored rounded exterior remains in progress. No completed photorealistic building or block is claimed.

Paused work is frozen: aircraft `.local-builds/aircraft-game-r1-source` + root `.local-builds/aircraft-root-r1-overlay/manifest.json`; supported extraction `.local-builds/supported-extraction-r3-final-source`; cabin contacts `.local-builds/vehicle-seat-contacts-r1-source`; responder diversity `.local-builds/character-diversity-r1-source`. These cohorts have native checks but are not fully visually accepted. Centenario/CARLA Tesla expansion source/conversion WIP stays preserved. Current dirty source includes overlapping unaccepted work: commit exact reviewed cohorts, never wholesale git add.

## Older 2026-09-12 status — superseded above

Newest pushed checkpoint **02224ce** repairs the Mini body shell while preserving damaged saves and adds device light/audio limits. Exact23 files in `.local-builds/road-cars-repair-r3-final-manifest.json`, build `.local-builds/road-game-r3` at4281, entry index-Q2X-Vqud.js. Full ten-car WebGL2 passed181 normal checkpoints/1824 transition frames, zero errors/warnings; Mini WebGPU passed19/150, zero errors/warnings. All193 native tests passed on r2 plus r3 Audio-only type/build. Root verified all666 staged runtime/test/build inputs against that source before commit. Updated full-roster WebGPU remains open.

Latest steering expands the vehicle scope: remove generic vehicles, use identifiable real planes/helicopters with manufacturer-sourced statistics, and add many branded cars including Lamborghini and Tesla. Road agent now owns aircraft asset/specification research and replacement while root handles extra branded road cars. Injury agents continue; this does not cancel the shared injury objective. Public Sketchfab metadata API works through curl; webpage fetch returns403. Root has a legitimate Objaverse asset-path index and Lamborghini primary metadata in `/tmp/codex-real-car-research`, not yet accepted assets.

Earlier **7a88733** is pushed to origin/main: ten licensed CARLA road cars, independent component physics/damage, old-save topology migration and modular startup/spawn/load preparation. Exact cohort `.local-builds/road-game-r1-source`, production entry `index-BFIauodF.js`; **192/192 tests and production build passed**. Normal WebGPU all ten cars passed 180 gameplay checkpoints/1,264 observed transition frames, but produced one glTF light-limit warning. Full WebGL roster remains pending. See `docs/road-car-roster.md`; final vehicle fidelity is not accepted. Mini paint seam repair is isolated in `.local-builds/road-cars-mini-r1-source`; Wrangler seat offset still needs both-body cabin review.

Earlier pushed checkpoints: **861fb99** persistent six-region injury state/specification, **6cb6312** 555 physical/damageable lamps and palms, **cb16de6** map/coast expansion. User preview 5174 still serves the stable street checkpoint. Do not replace a running preview without preserving its old hashed assets or deliberately rebuilding/reloading.

Latest user priority is a complete shared player/NPC injury system: separate left/right leg limp, arm handling impairment, strong torso slowdown, constrained falling, crawling, gradual healing, persistent fatal casualties, and correctly attached police equipment. The plan is `docs/body-injury-system.md`. Integration is **uncommitted** and broad: Player/Population/Officer/Combat/anatomical hits, real prone collision, low-roof rise checks, armor, health/regional recovery, seated injury behavior and saves. Current integrated frozen candidate `.local-builds/injury-game-r4-source` / `.local-builds/injury-game-r4`, served on **4271**, passes **224/224 tests and production build**. R4 adds immediate player-controller exclusion when damage arrives after the movement update. Normal controls on WebGPU passed NPC16 and player7 checkpoints with zero errors/warnings. The combined WebGL2 run passed22 checkpoints with zero errors but repeated an Audio.ts20kHz low-pass warning on a24kHz sample-rate device. A two-line Nyquist clamp is now in working source and road-r3; a clean fallback rerun is required. Evidence: regional-injuries-r4-webgpu, player-injuries-r4-webgpu and regional-injuries-r4-webgl.

The r3 all-frame physical studio captured 1,500 consecutive native/render frames with no errors/warnings, but visual review rejected the early fall-to-crawl handoff: the 1.2-second injury timer can interrupt a partially upright fall, producing an unsupported roll. The anatomical ankle correction also exposed a too-straight crawl target. `injury_ragdoll` is correcting grounded handoff timing/support and replacing skin-fitted boxes with convex skin hulls to avoid repeat-hit launches. `injury_poses` rejected r7 after its low side view exposed suspended thighs/trunk; r8 uses exact indexed skin support and an outward advancing knee. Its18,771,616 indexed checks and720-frame side capture passed with zero errors/warnings; root reviewed Jason’s side support improvement. Pose agent promoted only its exact Character/InjuryPose/test files; combined hull/handoff native and studio acceptance remain pending. CharacterSkinSupport.ts is being extracted as a physics-independent shared helper. Flat-road visual acceptance comes first; inclined-ground follow-up remains separate. R4 remains frozen; do not call those animation/fidelity gates complete. Supported critical/dead driver extraction is separately unaccepted WIP in VehicleInteractionPose.ts and its test. R4 deliberately uses the earlier healthy withdrawal baseline.

Root added saved civilian/police seat associations and includes their cars in saves. Restoring a replaced car rebinds its crew; wounded and dead police remain supported even after response reset or disabling police. Frozen `.local-builds/injury-seats-r2-source` is r4 plus these root changes and Audio clamp; its19 affected native tests and full225-test suite pass, with production build index-CvSjNUQ8.js. It is not yet the final combined injury cohort. Mid-entry save normalization remains an open edge case.

A global lighting fix is in working source/r4: Babylon 9.25 glTF completion raises all scene materials to total scene light count, overriding previous caps. `LightingBudget` enforces the official device-supported count before rendering, preserves explicit two-light water budget, handles material initialization order, and disposes its observers. Native regression passed. Mini/lighting/Audio follow-up road-r3 is frozen at4281; Mini WebGPU passed19 controls/150 transition frames with zero errors/warnings. All193 native tests passed on r2 and Audio-only r3 type/build passed. Full ten-car WebGL2 roster is running on r3; root reviewed the Mini side capture and repaired fender. Wrangler seat-contact work is isolated and unaccepted.

The weapon overhaul is also uncommitted: eight-slot wheel, timed drawing/stowing/reload, three licensed weapon models, compact-window drive-by, sniper scopes and minimal HUD. Older frozen weapons-v18 normal checks passed WebGPU; two later WebGL lifecycle reruns were clean after an initially unreproduced warning. Full new-car drive-by and complete animation fidelity remain pending. Six additional converted civilian source variants remain unintegrated/uncommitted.

One GPU browser at a time. Current agents: injury_poses (Character/IK/poses/extraction), injury_ragdoll (native constraints/handoff), road_cars (normal roster tests/Mini/Wrangler corrections). Root owns integration. Never stage all workspace changes. Stage exact accepted frozen cohorts, verify index bytes, and push every commit immediately. Full six-region map, all-object response, art fidelity and the 30-minute performance gate remain open; baseline 1% low was 19.45 FPS against a 30 FPS target.

## Latest integrated checkpoint — map/coast v15

See [map-coast-checkpoint.md](map-coast-checkpoint.md). All171tests and production build pass; normal UI map32 + coast13 checks on each backend,0errors/warnings. Production entry index-BHOH6UD5.js, worldv7. Western expansion, safe map pins/all20places, swimming/diving, water camera correction, world bounds and pause filler removal are integrated. Initial contact IK remains visibly deficient and is being rewritten; do not report animation parity.

In-progress source is ahead of this checkpoint: component_finish has isolated street-v16 with555 damageable lamps/palms awaiting normal audits; occupancy_finish is correcting all-frame entry/theft defects and has eight converted NPC sources pending runtime integration; edge_guard is converting licensed CARLA vehicle sources toward ten distinct detailed cars; root is implementing the weapon wheel/draw/scopes/window firing. Preserve each cohort and avoid staging all workspace files. One GPU browser at a time. Push every commit. Full goal remains active and the baseline1%low performance gate still fails.

## Repository and commands

The workspace and remote `https://github.com/Timmy0x/gpt-gta-6` were empty at inspection. Initialized local `main`, configured only that Git origin, installed pinned dependencies and retained lockfile. `npm test`, `npm run typecheck`, `npm run build`, `npm run dev`, `npm run preview` are the reproducible entry points.

During development the game server is at `http://127.0.0.1:5174` because an unrelated server occupied 5173. A frozen production snapshot under `/tmp/leonida-audit-build` is served at port 4175 for uninterrupted audits. Tests in `tests/browser-audit.mjs` and `tests/production-smoke.mjs` use that snapshot. Refresh it deliberately after a production build; editing source should not change an active audit.

## Current evidence

- Actual Havok behavior tests cover road suspension, acceleration, steering, solid/thin-wall collisions, deformation/glazing/debris, boat flotation/shore, helicopter ascent/descent, trainer runway takeoff, explicit recovery and identical physics outcomes under 30/60/144 Hz render cadence.
- Normal-control WebGL2 audit covers launch, walking, car entry, physical crash/damage, wanted reporting, switching, actual police arrival/reacquisition, weather save/load and pause. Independent screenshots and initial failure records are retained.
- Normal-control WebGPU production smoke covers car entry/driving/crash, repair/recovery/exit/switch, three-star pursuit, save/load, powered boat travel and helicopter takeoff.
- Initial WebGPU material warmup briefly stalls; later 1080p control tests are near 60 FPS. Raw measurements are short runs, not the required stability gate. Record actual JSON evidence rather than extrapolating.
- The two resource 404s were favicon requests. An explicit local SVG favicon fixes them; updated WebGPU smoke has zero console errors.

## Integrated corrections

Map search used `hidden` but button CSS overrode it; source now enforces `[hidden]`. Creative civilians were hidden by the ambient population budget; source now tracks explicit creative spawns separately. Both passed the final independent WebGL2 checkpoint retest. Siren NaN and lost police coordinates are fixed and retested. Detailed street props, marina-aligned boats, gamepad controls, 17-bone skinned characters and full vehicle component serialization are integrated. Both updated WebGPU smoke and independent WebGL2 checkpoint audits pass; all 29 behavior/rig/input/Havok tests and the production build pass. Read docs/readiness.md and the exact JSON records.

## Next gates, in order

1. Arrest/recovery is now verified; sandbox-assisted loss-of-contact completes search/cooldown/escape. Verify continuous driving/walking evasion next.
2. Continue loading/performance optimization and physical-device gamepad/menu verification. Missing favicon resources and both UI defects are resolved; production core paths pass on both backends.
3. Initial tested local checkpoint includes README, source/feature index and concrete evidence. Continue making tested commits; do not mark the full goal complete.
4. Deepen whole-world save/load and stable pedestrian IDs, interaction/exit safety, player/AI animation, combat/ragdoll, visible responders and tactical escalation. Keep feature status granular.
5. Acquire/create better licensed detailed assets; current procedural meshes do not meet requested final fidelity. Maintain provenance.
6. Expand adjacent finished districts following the geographic atlas, then all six planned regions with real streaming and complete content.
7. Perform isolated 30-minute traversal/pursuit/destruction profiling at 1080p, median60/1%low30 targets and bounded memory. Resolve failures rather than lowering targets.

The full source objective is copied to `docs/goal-objective.md`; read it and the feature index before continuing. Evidence of one passing loop never replaces the full map, feature, visual and stability requirements.

## Second checkpoint continuation (supersedes earlier next-step statuses)

70 tests and the production build pass. The combined code now includes police/Combat submodules, MovementQueries, directed waypoint Navigation, PhysicsInterpolation, Atmosphere/audio, and real ChunkResidency. The parent fixed integration defects in no-step jump buffering, recovery interpolation, autonomous grenade/ragdoll updates behind creative UI, malformed/duplicate save validation, dead-state controls, paused camera, race material disposal, destroyed-prop removal and burning-wreck extinction. Ragdoll reset is wired before reset/load; save captures every weapon and armor. Read the new police/combat/movement/world design documents.

Current browser commands: `node tests/integrated-checkpoint.mjs webgpu` and `node tests/police-browser-audit.mjs`. The frozen production snapshot remains4175; update `/tmp/leonida-audit-build` only between active audits. The world agent's4181 helper is stopped. Combined audit uses normal UI/keyboard/mouse with read-only test diagnostics; its earlier failed harness records are retained.

Normal armed-threat urban evidence is now retained in `verification-police-checkpoint.md`; both combined renderer audits pass. Next priorities: improve blocked/oscillating urban AI routes, capture close-up tactical behavior, and run isolated30-minute mixed-world performance/memory profiling; acquire/create higher-fidelity licensed assets; continue contiguous district/regional construction following the atlas with modular network assets, regional water/terrain and actual military/emergency/activities systems. Do not treat this expanded local district as the full planned map. The user subsequently authorized frequent commits and pushes on 2026-09-09. Push tested checkpoints to origin/main; deployment configuration still awaits a separate decision.

## Third-checkpoint continuation

The user explicitly superseded the no-push restriction: commit/push frequently, and push each tested checkpoint. Existing dcc7bca/ce382f3 commits are on origin/main. Do not configure deployment without a separate decision.

Current changes include network World/PackageResidency and export-time WorldBuilder; optional world.ready/preparePosition lifecycle is wired in main. The native browser fetch receiver and invalid shared-box merge indices were fixed, all world geometry indices validated, sign PNGs preserved at1024px, and PBR materials rebound to scene image processing. Current manifest build authored-860409-v3 contains84packages,171materials,46PNGtextures,1221meshes,57,932,888CPU geometry bytes and13,132,677compressed package/material bytes.

Root added VehicleEquipment, Garage, bounded FrameHistory, animated/detachable doors, lights/brake/siren behavior, paint persistence, seated pose clearance and plane turn correction. RestrictedFacility/PoliceDirector improvements, production Havok tests, licensed GIS and debranded CarConcept asset preparation also landed. CarConcept is prepared only, not runtime-integrated.

All94 tests and production build pass. Parent WebGPU and WebGL2 checkpoint-3 audits each pass12stages with zero unexpected errors. Real HTTP evidence is refreshed. Frozen current production directory is /tmp/leonida-checkpoint-3-build on4176; the original4175 snapshot is ce382f3 and should stay archived. Runtime/server processes were interrupted by a usage-limit event;4176 was restarted. Subagents reference_atlas and vehicles errored with account usage limit and world became pending; parent continued integration. Do not invent completed subagent reviews after that interruption.

The old30-minute baseline stopped15m51s from harness aircraft-recovery errors; plane banking was separately proven wrong then fixed. Corrected180-second flight smokes passed before the final rendering corrections. Full30-minute validation of the new frozen build remains next, alongside the existing combined combat/save/load normal-control tests. Keep final evidence separate from preserved startup/report-timing/harness failures.

Final third-checkpoint gate: both existing combined combat/save/load renderer audits pass eight normal-control stages with zero errors, in addition to both new 12-stage audits. Logs use `checkpoint-3-*`. All94 unit tests and production build pass. The stability harness now awaits destination package preparation for its explicit fixtures and UI fast travel/load. Next run the new frozen build for30minutes, then integrate the prepared detailed car asset without modifying that frozen snapshot.


## Completed baseline and in-progress detailed car

1387665 was committed and pushed to origin/main. Its frozen4176 build completed the full30-minute harness with zero browser errors and no transport recoveries, but performance failed:59.52median/19.45slowest1%FPS and1026.3ms maximum stall. Heap188–984MB, final697MB; settled retention is unproven. Evidence `stability/baseline-2026-09-09T15-52-42-401Z` and performance-stability.md retain limitations. Largest stalls correlate with save/load; profile before claiming a cause. Do not repeat a final benchmark while concurrent CPU test/build jobs are active.

Next working changes add `ConceptCarAssets`, `LatticeDeformation`, the concept VehicleKind, lazy verified GLB loading, transformed wheel/door/cover hierarchies, independent mutable materials with shared immutable textures, driver pose, compact damage saves and credits. All97 behavior tests and builds pass. CPU browser material tests prove actual PNG loading,23materials/120childmeshes/213347triangles and8stable creation/removal cycles. This remains uncommitted until both normal renderer audits finish. A frozen current car build is on4177 at /tmp/leonida-concept-checkpoint-build; earlier variants are archived under /tmp/leonida-concept-before-*.

The first WebGPU car audit verified driving, physical crash, visible cover loss and restored dents. Initial continuation assertions were premature/used a zero-size HUD box; evidence is retained. Main now keeps the welcome screen until asynchronous saved vehicle/world loading completes. Driven-car headlamps have priority over closer unoccupied traffic in the fixed beam pool. Both final renderer audits must pass before committing and immediately pushing this next checkpoint. Continue performance profiling, higher-fidelity characters/world assets and all remaining geography/system scope afterward.

Detailed-car verification finished: both final renderer audits pass all13stages, zero unexpected errors. Source now awaits saved-game restoration before dismissing welcome, occupied vehicles own headlamp priority, and Vite prebundles the lazy loader to prevent first-spawn reload. User now prioritizes aircraft stuck on ground, rapid respawns and much more realistic cars/characters. All three subagents recovered and are working: vehicles normal aircraft controls/spawns; reference_atlas casualty lifecycle (player recovery currently resets all civilians/guards); world licensed character replacement. Parent owns integration and main save/reset wiring.


## Visual/casualty checkpoint continuation (supersedes previous runtime handles)

8584c66 (detailed car) and 02e4363 (aircraft launch and controls) are committed and pushed. Current verified changes add Rocketbox player skins, CC0 HDR lighting, persistent NPC casualties and collision-free ground spawns. All 113 tests and production build pass; integrated WebGL2 car 13-stage and WebGPU casualty/character 20-stage records have zero errors. See readiness.md and dedicated documents. Frozen build `.local-builds/visual-casualty-v1` is served on4187; restore5174 to this tested directory for user play. Previous /tmp snapshots and process handles may have expired; inspect processes before relying on them.

Character verification agent completed 11 actual-skin checks and documentation. Gameplay review found a real barrel/camera mismatch and left Character.aimToward, Player calls and a combat regression unstaged for a separate normal-control verification. Vehicle LOD agent left 61,879-triangle derivatives and batching/review scripts; these are not runtime integrated or visually accepted yet. Both latter agents stopped with account usage limit. Do not assume their pending checks passed. Parent owns review, integration and frequent commit/push. No usage-reset credit has been authorized.

The full goal remains active. Most geographic content, realistic vehicle/NPC variety, animation and world art remain incomplete; the recorded 30-minute performance gate still fails. Continue with aiming validation, car LOD visual/runtime integration and ordinary-play detailed cars, then broader licensed assets/world material improvements and full-scope expansion.


Aiming follow-up: parent verified the subagent's arm correction against frozen `.local-builds/aiming-v2` (module index-BZcSSHf8.js). Both renderer normal-control 10-stage audits and the 16-stage WebGPU lethal-shot/BUSTED/save/fresh-Continue flow pass with zero errors. All114 tests and build pass. Held-C crouching was corrected in the harness; a tap does not test crouching. This change is ready for a separate push. The next uncommitted work switches the detailed-car cache to the reviewed batched derivative and introduces a starter/two-traffic placement; its current tests pass but production normal-controls validation remains pending.


## Detailed street-car checkpoint — 2026-09-09

476e191 (licensed players/HDR/casualties/safe spawning) and c12324c (camera-aligned arms/weapons) are pushed to origin/main. The next tested checkpoint switches the default car to the subagent's reviewed batched derivative, uses it for the starter and two ambient cars, caps total detailed cars at six, and evicts surplus ambient detailed cars when restoring six owned cars. Parent fixed the newly observed powered-reverse-after-exit bug; successful exit releases inputs and parks ground cars. Rejected exits preserve controls.

All114 tests and build pass. Actual-material create/remove cycles remain stable. `detailed-street-webgpu-final-pass` and `detailed-street-webgl-final-pass` each contain ten passing stages on module index-DmvLSStP.js. `concept-webgpu-street` and `concept-webgl-street-final` have fourteen passing stages each; only the former predates the separately covered exit correction. All report zero unexpected errors. Initial exit movement and the failed flat-coordinate test fixture are retained. Node fetch rejects port4190, although browser checks worked there; the same frozen files are served on4191 for Node-assisted audits.

Current final frozen directory is `.local-builds/detailed-street-v4`; use it for the user's5174 page. All previous /tmp process handles should be revalidated. Next: improve stretched/world material UV scale in src/world/authoring/WorldBuilder.ts (road currently38×38 repeats regardless of physical width/depth; sand42×90 on55×1250m), acquire licensed surface textures and more realistic vegetation/NPC/vehicle variants, then continue full map/system scope and profile failed performance gate. World exporter is scripts/world/export.mjs; changing authoring requires regenerating packages and validating native indices/streaming. No world UV edits have been made yet.


## World texture-scale follow-up

eae7c9f (detailed street cars and parked exit) is committed and pushed. Parent then corrected WorldBuilder asphalt/sand UV scale using authoring/metreUVs.ts, regenerated world packages as authored-860409-v4-metre-uv, and verified all115 tests/build. Both normal WebGPU and WebGL2 street/beach audits pass with actual loaded geometry at0.75m asphalt/0.5m sand per tile. Before stretched ranges were0.289–15.526m asphalt and0.611–29.762m sand. Original source grain textures are unchanged. All colliders/navigation/indices/normals match; position rounding is below5.59e-9m. Numeric exporter geometry IDs change; package hashes are updated.

Current final frozen directory `.local-builds/metre-uv-v5` has the same application module index-DmvLSStP.js but a new world manifest SHA f6aaa34892099011171b7968619778ee55713441a01f3b14eda73307bdbef81b. Use this frozen directory for5174 after pushing. Before/after evidence and failed normal-length harness assumptions are retained. No GPU benchmark is running. Subagents previously stopped due usage limits; no reset authorized. Next visual work should acquire licensed detailed PBR surfaces and vegetation/NPC/vehicle variants, improve atmospheric sky and reference world art, while retaining the full outstanding map/system/performance scope.


## Photo surfaces checkpoint, 9 September 2026

Added verified local CC0 Asphalt 02 / Sand 03 PBR maps at source metre scale; normal WebGPU and WebGL2 street/beach audits pass with zero errors. See `docs/photo-surfaces.md` for fingerprints and screenshots. Road detail is improved; selected sand is too dark for dry tropical beach and needs a paler source replacement. Sky and civilian integration plus aircraft seat corrections are parallel work, not part of this surface checkpoint. Full goal and the failed long-session 1% FPS target remain open.


## Coastal detail v8 and latest user priorities

Graphics checkpoint: detailed nearby civilians (12 cap), native atmospheric sky/stars, dry Dense Sand replacement and corrected aircraft cockpits. Frozen `.local-builds/coastal-detail-v8`, port4198, `index-CxJrQY1k.js`, world `authored-860409-v6-dry-sand`;125 tests/build pass. Sky16 stages, aircraft32 checks and fatal civilian31 stages across both renderers pass. Root is committing/pushing this independently of new work.

Latest user asks: realistic weapon facing/handling (backward hip fire), visible carjacking/driver removal and traffic escape, no quick recovery from serious NPC injury, substantial connected map growth, minimal controls text, collision for visible vehicle/scenery components, realistic water/swimming/shoreline and protection against falling off world edges. Keep all these requirements active alongside full goal.

Agent assignments: vehicle_lod owns VehicleOccupancy/Player enter-exit/Population traffic adoption and panic driving; root owns Player weapon-facing sections and Combat.fire early gate. character_verify owns injury-aware Ragdoll/Character, injury callbacks and persistence v2 incl living incapacitated NPCs, Population hurt/casualty sections. gameplay_review owns a pure western VC-INNER expansion module (X−1100…−450,Z−300…600, ~0.585km²) and bounds/ground metadata. Root integrates main/UI/world export, physical component gaps and water/boundary work. Source files have concurrent new work; staging the v8 Character and Population uses exact known index blobs to avoid committing incomplete follow-ups.

Two future CC0 sets are prepared but unintegrated: White Stucco (~2m) and Concrete Floor03 (~2.5m),5.15MB combined. Preserve pastel facade tint when integrating; concrete is darker/warmer than the old cream sidewalk and needs actual visual review. Optional new vehicle acquisition was paused for user priorities; legacy Khronos Buggy was rejected because its reuse license was missing/unsuitable.


## Interaction checkpoint continuation

The next tested checkpoint uses `.local-builds/interactions-v10` at4201 and its exact source `.local-builds/interactions-source`; staged source was independently reconstructed in `.local-builds/interactions-index-source` and produces the identical e95167f… entry hash. All135 tests pass. Injury, weapon-facing and both32-total carjacking stages are recorded in readiness and dedicated documents. Publish every commit as authorized.

Shared worktree deliberately contains later map/water/exterior changes excluded from the interaction commit: v7 InnerCity export138 packages/730colliders, Coast physical banks and support terrain, Ocean native water, Swimming depth/dive/breath, WorldBoundary, vehicle component colliders/door sweep and police local vehicle detours. Current coastal candidate `.local-builds/coastal-v11` serves4202; normal water audit is in progress. Its exact code is not final and it must not be called a completed feature/visual/performance gate. Rebuild after all integration fixes and verify normal controls on both backends before its checkpoint. Full goal remains active; six-region scope and the failed long-run performance target remain outstanding.

Agent tree can be cleared by goal-turn interruption while child Chrome/Node processes survive. Inspect live agents and exact task-owned process handles before resuming or spawning replacements; keep one GPU browser at a time. Current agents are occupancy_finish (next interaction-pose improvements, wait for root source freeze), component_finish (fine collision and normal-control review), edge_guard (boundary complete).


## Exterior checkpoint and active expansion

Next checkpoint contains only physical road-vehicle components, door clearance/exit positioning, police vehicle detours and verification. Exact staged source `.local-builds/exterior-index-source` passes143tests/build. Normal integration evidence is under `docs/evidence/vehicle-exterior/occupancy-coastal-v12-{webgpu,webgl}` (16checks/148samples each,0errors/warnings). User still requires each commit to be pushed immediately.

Active user steering now explicitly includes highly realistic ALL vehicle classes, seeded diverse NPC appearances (skin tones, gender presentation, age, body/face/hair/clothing variation), physical damage for ALL objects including lampposts and trees, and useful map travel to every point. Do not treat any bounded checkpoint as full parity. Current agents: component_finish implements durable physical/damageable authored lamps/palms; occupancy_finish is completing v13 IK visual checks and acquiring diverse licensed civilian models; edge_guard fixes native underwater Fresnel/refraction. Root owns map pan/zoom/arbitrary safe travel, swimming posture, main integration and full normal playtests.

Frozen v13 at4204 includes168passingtests/map/swim/IK; its first map audit found a search-harness ambiguity (two Mercado locations) and a real general self-capsule query issue. Root fixed MovementQueries to exclude only its own shape during synchronous queries, preserving vehicle-ignore behavior and restoring masks.15 movement/occupancy/coast/travel tests pass. v14 source is prepared from v13 plus these fixes and camera-medium fog ordering; awaiting final water source before build. Shared worktree also includes larger streetprop/NPC changes that MUST stay out of this frozen candidate until coherent.


## Physical street checkpoint (2026-09-09)

The street-v18 functional slice adds physical/destructible ownership to 264 lamps and 291 palms, preserves original batched geometry, and saves fallen transforms. Both WebGPU and WebGL2 normal-input audits passed ten checks with zero errors/warnings. Native query exclusion now ignores the player capsule before selecting a target; fallen objects retire before their streamed support disappears. See docs/street-object-destruction.md and docs/evidence/street-objects/staging-v18.json for exact source/evidence. This slice does not complete all-object damage or visual fidelity. The separate weapon catalog, vehicle assets, entry choreography and regional injury system remain in progress.
